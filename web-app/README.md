# CargoCheck web app

The full documentation, quick start, configuration and deployment steps are in the
[repository README](../README.md).

Run from the repository root: `python -m streamlit run web-app/app.py`
Run the tests: `cd web-app && python -m unittest discover -s tests`
